root            = ,{{ (ds "colors").background }}
border          = {{ (ds "colors").color15 }},{{ (ds "colors").background }}
window          = ,{{ (ds "colors").background }}
shadow          = ,{{ (ds "colors").background }}

title           = {{ (ds "colors").accent }},{{ (ds "colors").background }}

button          = {{ (ds "colors").background }},{{ (ds "colors").accent }}
button_active   = {{ (ds "colors").background }},{{ (ds "colors").cursor }}
actbutton       = {{ (ds "colors").cursor }},{{ (ds "colors").background }}
compactbutton   = {{ (ds "colors").foreground }},{{ (ds "colors").background }}

checkbox        = {{ (ds "colors").color1 }},{{ (ds "colors").background }}

entry           = {{ (ds "colors").color2 }},{{ (ds "colors").background }}
disentry        = {{ (ds "colors").color7 }},{{ (ds "colors").background }}
textbox         = {{ (ds "colors").color2 }},{{ (ds "colors").background }}
acttextbox      = {{ (ds "colors").accent }},{{ (ds "colors").background }}

label           = {{ (ds "colors").color6 }},{{ (ds "colors").background }}

listbox         = {{ (ds "colors").color2 }},{{ (ds "colors").background }}
actlistbox      = {{ (ds "colors").accent }},{{ (ds "colors").background }}
sellistbox      = {{ (ds "colors").color1 }},{{ (ds "colors").background }}
actsellistbox   = {{ (ds "colors").background }},{{ (ds "colors").cursor }}
